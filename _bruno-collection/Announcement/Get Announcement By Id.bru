meta {
  name: Get Announcement By Id
  type: http
  seq: 5
}

get {
  url: {{GATEWAY_URL}}/api/announcements/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{ACCESS_TOKEN}}
}

script:post-response {
  if (res.status === 200) {
    console.log('✅ Announcement retrieved:', res.body.id);
  } else {
    console.error('❌ Announcement not found:', res.status);
  }
}